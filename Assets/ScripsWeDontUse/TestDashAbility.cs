using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class TestDashAbility : MonoBehaviour
{
    // Reference to the SlayPlayerMovement script
    private PlayerMovementCombined playerMovement;

    private void OnTriggerEnter(Collider other)
    {
        // Check if the player has entered the trigger
        if (other.CompareTag("Player"))
        {
            // Get the SlayPlayerMovement component from the player
            playerMovement = other.GetComponent<PlayerMovementCombined>();

            if (playerMovement != null)
            {
                Debug.Log("Collectable collected!"); // Debug statement
                playerMovement.Dash(); // Call the method to enable dash
                Destroy(gameObject); // Remove the collectible
            }
        }
    }
}*/
